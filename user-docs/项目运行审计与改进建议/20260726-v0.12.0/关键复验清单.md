# 关键复验清单

日期：2026-07-26

目标是在不过度重复检查的前提下，覆盖本轮最可能导致实例启动失败、卡死、误报成功或破坏用户数据的路径。命令应在 `/home/deco108/AI-Agent` 执行，不读取或打印真实凭据。

最终执行记录：502 项 pytest 全部通过；Ruff check、限定源码范围的 Ruff format check、compileall、pip check、`git diff --check` 均通过；隔离 XDG 的版本、帮助、初始化、缺 Key 前置失败和 launcher 冒烟通过。在线 DeepSeek 验收按用户要求延期，未读取或调用真实 Key。

## 一、快速聚焦门

```bash
cd /home/deco108/AI-Agent
.venv/bin/ruff check .
.venv/bin/ruff format --check .
.venv/bin/python -m pytest \
  tests/test_budget_resilience.py \
  tests/test_progress_artifact.py \
  tests/test_vector_lazy.py \
  tests/test_memory.py \
  tests/test_optimizer.py \
  tests/test_cli.py \
  tests/test_tools.py \
  tests/test_event_state_contracts.py \
  tests/test_interface_contracts.py -q
```

必须重点观察：

- ToolManager 构造签名没有破坏现有调用方。
- shell 参数中的空格、引号、分号、`$()` 不会改变参数边界或执行额外命令。
- 文件锁在冲突、异常和退出时释放；Windows 分支至少有 mock/条件测试。
- Vector 构造阶段零导入/零下载。
- Artifact 的损坏 ZIP、路径穿越、重复 member、CRC/XML、空正文和高压缩比均失败。

## 二、Runtime 与迁移门

```bash
.venv/bin/python -m pytest \
  tests/test_runtime.py \
  tests/test_runtime_convergence.py \
  tests/test_runtime_event_migration.py \
  tests/test_session.py \
  tests/test_config_migration.py -q
```

若实际测试文件名不同，先用 `rg --files tests | sort` 查找，不要猜命令。验证以下语义：

1. 每类模型请求在网络前计入预算并 checkpoint。
2. 预算耗尽后不再发请求、不执行工具，Session 保存失败原因并可 Resume。
3. 旧 schema 6 能迁移到 schema 7；未知未来 schema 仍拒绝。
4. 旧计划 ID 可映射 `step_type`；新计划不能预填完成。
5. completed、failed/resumable、exception、KeyboardInterrupt 的 CLI 退出码准确。
6. Deliverable 的 create/delete/undo/reparse lineage 不误报。

## 三、全量门，只运行一次

聚焦通过后执行一次：

```bash
.venv/bin/python -m pytest -q
.venv/bin/ruff check .
.venv/bin/ruff format --check .
.venv/bin/python -m compileall -q agent tests scripts
.venv/bin/python -m pip check
git diff --check
```

记录实际 collected/passed/failed/skipped、耗时和环境，不能沿用 v0.11.0 的“454 passed”数字。

## 四、隔离 HOME 的无 Key/Vector 冒烟

使用测试夹具或临时隔离 HOME/XDG；不得读取现有 `~/.config/deep-agent`：

| 检查 | 预期 |
| --- | --- |
| 无 `DEEPSEEK_API_KEY` 运行普通任务 | 快速退出 1 |
| Chroma extra 已安装 | 仍不下载 ONNX、不初始化 collection |
| 新默认配置 | `memory.vector_enabled: false` |
| 旧配置显式 true | 构造阶段不加载，首次真实向量操作才加载 |
| 输出和日志 | 不包含 Key、Cookie、Token、Secret |

## 五、真实 PTY 门

任何交互 UI 改动必须用真实 PTY 或等效终端检查：

1. 启动 `agent` 后输入普通任务并按一次 Enter。
2. 提交后立即显示处理中状态和可信进度。
3. 空 Enter 有明确提示。
4. `Ctrl+C` 回到可恢复状态，不破坏输入行。
5. `/resume` 或指定 Session 恢复；`/exit` 返回 0。
6. ANSI prompt 的不可见字符用 `\001`/`\002` 包裹，CJK/emoji 宽度不破坏行编辑。

只使用脱敏、隔离的测试 Session，不复制真实用户运行数据。

## 六、打包与干净安装门

```bash
cd /home/deco108/AI-Agent
.venv/bin/python -m build
```

在临时虚拟环境安装刚生成的 wheel，然后验证：

```text
agent --version -> deep-agent 0.12.0
agent --help    -> 退出 0
agent doctor    -> 离线能力检查完成且不泄密
agent init      -> 隔离临时项目成功
pip check       -> 无冲突
```

同时核对 wheel/sdist 中包含 release notes、必要包文件和正确依赖下限，不包含 `.project-agent`、Memory、Session、日志、缓存或凭据。

## 七、文档与归档门

- 根目录只保留最新版 `README.md`、工作日志和最新版版本化 Word。
- v0.11.0 使用说明移入 `老版使用说明/`；v0.11.0 Word 工作日志移入 `老版工作日志/`。
- 本轮四份输入材料保存在 `历史资料/改进建议/20260726-v0.12.0/`，四个 SHA-256 与归档 README 一致。
- 根 README、源码 README、release notes、Markdown 工作日志和两份 0.12.0 Word 的版本、schema、默认值、退出码和测试数字一致。
- Word 执行 ZIP 完整性、重新解析、标题/关键章节和 SHA-256 镜像核对。

## 八、GitHub 发布门

最终提交后：

```bash
git status --short
git rev-parse HEAD
git ls-remote origin refs/heads/main refs/tags/v0.12.0 'refs/tags/v0.12.0^{}'
```

必须满足：

- 工作树没有遗漏的发布文件；不覆盖用户无关改动。
- `main` 和解引用后的 `v0.12.0` 指向同一发布提交。
- GitHub Actions 对该提交完成并成功。
- 最终回复记录版本、完整提交哈希、测试结果、远端核验和文档位置。

## 九、在线验收边界

在线 DeepSeek 任务本轮明确延期：用户 API 余额已经耗尽。余额恢复且用户确认后，只执行一次有上限的代表性任务：

- 明确最大请求数、Token 和时间。
- 保存 `git_head`、dirty 状态和脱敏 run manifest。
- 成功以 Session completed、Artifact verified 和真实验证证据为准。
- 失败后停止并分析，不连续重跑消耗余额。
