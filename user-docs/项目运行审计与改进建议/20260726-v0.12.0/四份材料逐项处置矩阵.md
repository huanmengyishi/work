# 四份材料逐项处置矩阵

日期：2026-07-26

审查对象：

- 文档入口：`/mnt/d/detail/deepseek`
- 源码：`/home/deco108/AI-Agent`
- 审查启动时源码 HEAD：`7b575fa21da772379122b904b77ce25a79192267`
- 当前候选版本：`0.12.0`
- 离线验证：502 项测试及静态、依赖、隔离实例门均通过；在线 DeepSeek 验收按用户要求延期
- 当前 AgentState schema：`7`
- 当前核心接口契约版本：`3`

状态含义：

- **已实现并验证**：这是矩阵编制阶段的原状态标签；截至最终离线收口，这类条目已通过 502 项全量及相应聚焦回归，公开发布仍以 commit/tag/远端核验为准。
- **部分实现**：安全核心已落地，材料提出的完整范围仍有剩余。
- **原判断不成立**：材料对 v0.11.0 已有行为的判断与代码不符，未按错误前提重写。
- **不采纳**：违反项目约束、许可证边界或会引入不受控副作用。
- **延期**：有价值，但不是本次安全发布的必要条件。

## 一、长任务可靠执行与 Resume

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| 协议纠正次数、异常 finish 恢复次数不能配置 | 已实现并验证 | 新增有界配置，默认值保持兼容；只控制协议恢复，不扩大网络重试 | `agent/resilience.py`、`agent/config.py`、`agent/runtime.py` |
| Runtime 缺少错误分类 | 已实现并验证 | 将中断、协议、上下文、认证、限流、网络、超时、权限、资源等错误分类并保存安全标量 | `agent/resilience.py`、`agent/runtime.py` |
| Runtime 再实现网络退避、熔断和自动重试 | 不采纳 | `DeepSeekClient` 已唯一负责 HTTP 重试、Key 轮换和退避；Runtime 重试会重复请求并可能重放副作用 | `agent/deepseek.py`、`agent/resilience.py` |
| API 失败时切换备用 Provider | 不采纳 | 与“DeepSeek 是唯一推理模型”强制约束冲突 | `AGENTS.md` |
| Resume 会从头执行、丢失计划、节点和工具证据 | 原判断不成立 | v0.11.0 已持久化计划、步骤、工具证据和检查点；每 turn 重置的是活性/停滞额度，不是已完成工作 | `agent/runtime.py`、`agent/state.py`、`agent/session.py` |
| 每 N 轮 checkpoint | 不采纳材料中的替换方式 | 现有逐模型请求预留与逐工具批次原子 checkpoint 更强；降低保存频率会扩大中断窗口 | `agent/runtime.py`、`agent/session.py` |
| 模型请求、Token、执行时间总预算 | 部分实现 | 已新增每 turn 的请求、Token、时间硬预算，请求前 checkpoint，耗尽后保存可 Resume 失败态；跨多个 Resume 的生命周期累计预算仍未实现 | `agent/budget.py`、`agent/runtime.py`、`agent/config.py` |
| 最大成本金额和 Artifact 总大小预算 | 延期 | 金额需要版本化价格表；Artifact 已有单文件/附件上限，但尚未形成统一任务级产出总预算 | `agent/artifact.py`、`agent/tools/manager.py` |
| 任务百分比与 ETA | 部分实现 | 只按已满足步骤权重给出可信百分比；不猜测步骤内部进度或 ETA | `agent/progress.py`、`agent/console.py`、`agent/session.py` |
| 中断后自动触发 Resume | 延期 | 自动 Resume 可能在外部副作用状态未知时重复执行；继续保留显式 Resume 和可审计状态 | `agent/runtime.py`、`agent/session.py` |

## 二、计划、Task Graph 与任务状态

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| `PlanStep` 缺少语义、父节点、轮次估算、Artifact、验证规则和权重 | 已实现并验证 | schema 7 增加 `parent_id`、`step_type`、`estimated_tool_rounds`、`artifact_ids`、`validation_rules`、`progress_weight`，并限制范围 | `agent/state.py`、`agent/planner.py` |
| Convergence 硬编码固定步骤 ID | 已实现并验证 | 优先按 `step_type` 判定阶段，并兼容旧 ID | `agent/convergence.py` |
| 计划粒度不使用任务类型、规模和风险 | 部分实现 | 不同变更/审查/研究任务使用不同语义标题、权重和轮次估算；尚未引入开放式策略插件 | `agent/task_plan.py` |
| 新计划可预填 completed/skipped/failed | 已实现并验证 | 新计划只接受合法初始状态，防止模型伪造已完成进度 | `agent/tools/manager.py`、相关测试 |
| 层级 children/subplan 完整执行 | 部分实现 | 已有父节点元数据和校验，但没有递归子计划调度器 | `agent/state.py`、`agent/planner.py` |
| 动态 insert/split/replan | 延期 | 需要明确 revision、冲突处理和完成证据继承规则，不能直接照搬材料伪代码 | `agent/planner.py` |
| 真正并行步骤或子 Agent | 延期 | 当前只允许显式 `concurrency_safe` 的只读工具并发；写步骤/子 Agent 并行缺少隔离、权限和合并协议 | `agent/runtime.py`、`agent/tools/manager.py` |
| 计划步骤数直接放大到 64 工具轮次 | 不采纳 | 机械放大可能重现百万 Token 卡死；使用独立请求/Token/时间硬预算更可靠 | `agent/budget.py`、`agent/convergence.py` |
| completion criteria 依赖模型自述 | 部分实现 | 验证步骤可要求真实 managed validation/document parse 证据；通用自然语言 criteria 尚未变成可执行 DSL | `agent/runtime.py`、`agent/task_plan.py` |
| 新增统一 AppState/TaskManager 并替换 AgentState | 延期 | 现有 `AgentState` 是稳定架构边界；大规模替换需独立迁移版本，不能与可靠性修复混做 | `agent/state.py`、`agent/contracts.py` |

## 三、Artifact 与结果验证

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| 缺少结构化 Artifact 验证 | 部分实现 | 新增受限 `ArtifactSpec`/`ArtifactVerifier`，从受管 `ToolResult` 证据验证，不绕过 Tool Manager 直接读文件 | `agent/artifact.py` |
| JSON/YAML/Python 语法、非空和大小检查 | 已实现并验证 | 使用标准解析器/PyYAML，输入与错误输出有界 | `agent/artifact.py`、`tests/test_progress_artifact.py` |
| DOCX 可打开、OPC/XML/CRC、空内容和 Zip Bomb 检查 | 已实现并验证 | 校验必需包成员、安全路径、CRC、XML、正文和压缩比/解压总量 | `agent/artifact.py` |
| Runtime Word gate 检查实际解析内容 | 已实现并验证 | `document.parse` 生成不含正文的受管 receipt；Runtime 只验证路径、格式、大小、哈希、完整性、OPC/XML/CRC 和非空正文证据 | `agent/runtime.py`、`agent/artifact.py`、`agent/tools/document.py` |
| Session 增加 Progress/Deliverables/Verification | 已实现并验证 | Markdown 摘要已增加三部分并基于受管工具回执 | `agent/session.py` |
| Undo/Delete 后 Deliverable 不误报 | 已实现并验证 | Session 回放 apply/delete/undo 最终状态；撤销创建或最终删除的文件不再显示 generated/verified | `agent/session.py`、`agent/runtime.py` |
| 大文档按章节拆分、章节级 Artifact | 延期 | 需要先定义章节一致性、合并、回滚、重复标题和总量上限；本轮不加入未经验证的自动拆分 | `agent/tools/document.py` |
| 超大文件分块部分写入 | 不采纳当前方案 | 分块写会破坏 `file_diff -> file_apply -> file_undo` 原子快照语义；应先设计事务式方案 | `agent/tools/file_edit.py` |
| AdaptiveCompactor 按模型推测重要性分配 | 延期 | 当前确定性压缩容易复验；错误/产物启发式需要基准数据后再引入 | `agent/convergence.py` |
| ContentMatch 自动判断是否满足用户语义 | 延期 | 纯规则容易误判，额外模型判定会增加请求；优先使用明确路径、语法、结构与真实测试证据 | `agent/artifact.py`、`agent/runtime.py` |

## 四、Memory 2.0

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| `kind` 无枚举约束 | 已实现并验证 | 新写入严格使用 `MemoryKind`；旧数据库未知类型仍可读取，避免破坏迁移 | `agent/memory.py` |
| 无容量控制 | 已实现并验证 | 新增条数/估算负载上限、扫描上限和 dry-run；只有显式 `apply=True` 才淘汰 | `agent/memory.py` |
| Correction/Decision 可能被淘汰 | 已实现并验证 | 容量维护和过期维护均保护持久类型 | `agent/memory.py` |
| 置信度永不变化 | 部分实现 | 只接受显式、幂等、带 feedback ID 的正/负反馈并设置上下限；普通读取不会自行抬高置信度 | `agent/memory.py` |
| 自动按使用次数加分和按时间衰减 | 不采纳 | 使用频次不等于事实正确，自动衰减可能损害重要决策；保留显式证据反馈 | `agent/memory.py` |
| LLM 自动抽取、分类并默认开启 | 延期/默认关闭 | 会增加 DeepSeek 请求、隐私面和预算；重型能力必须默认关闭 | `agent/memory_pipeline.py`、`agent/config.py` |
| 跨项目知识复用 | 已有基础/部分实现 | Memory 已支持 `project_id=None` 的全局记录和 `--global-memory`；完整模式检测器延期 | `agent/memory.py`、`agent/cli.py` |
| Memory 搜索、列表、统计、维护 | 已有基础 | v0.11.0 已提供 search/add/list/stats/maintain；export/import 与高级过滤延期 | `agent/cli.py` |
| Vector 默认开启且启动即加载 | 已实现并验证 | 新安装默认关闭；旧配置显式开启时也延迟到首次向量操作 | `agent/config.py`、`agent/vector.py`、`agent/memory.py` |
| 去重升级为向量语义判断 | 延期 | Vector 为可选重型依赖，不能成为核心写入前置条件；先保留有界确定性去重 | `agent/memory.py`、`agent/vector.py` |

## 五、观察性优化而非自动改写策略

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| 缺少任务性能历史 | 已实现并验证 | 新增 SQLite 安全标量历史、`run_id` 幂等和容量限制 | `agent/optimizer.py` |
| 终态事件接入性能采集 | 已实现并验证 | 只观察 terminal event，不保存 Prompt、正文、参数值或凭据 | `agent/event_pipelines.py`、`agent/optimizer.py` |
| 自动调整模型路由和轮次阈值 | 不采纳当前版本 | 观测样本不足时自动调参会造成不可复现回归；DeepSeek-only 路由也不能变成多 Provider | `agent/optimizer.py` |
| 自动 A/B 分流真实用户任务 | 不采纳当前版本 | 未经同意改变执行策略和成本，不符合可预测性与预算要求 | 无代码 |
| 成功/失败后默认调用模型反思 | 不采纳默认开启 | 重型能力默认关闭，且会产生额外 API 成本；现有反思保持 opt-in | `agent/reflection.py`、`agent/config.py` |
| 根据性能历史给出只读报告 | 后续建议 | 可基于已保存安全标量生成显式、人工确认的建议，不自动改配置 | `agent/optimizer.py` |

## 六、全面代码审查与工程一致性

| 材料事项 | 处置 | 当前实现或理由 | 位置 |
| --- | --- | --- | --- |
| `shell=True` 使用空格拼接 | 已实现并验证 | 改为 `shlex.join()`；特殊字符保持单参数且不误执行的回归通过 | `agent/tools/base.py` |
| `fcntl` 限制 Windows | 已实现并验证 | 配置、项目、Daemon、Queue、Session 使用统一 POSIX/Windows 锁；无 `fcntl` CLI 导入及 Windows 锁分支模拟通过 | `agent/file_lock.py`、`agent/config.py`、`agent/project.py`、`agent/daemon.py`、`agent/task_queue.py`、`agent/session.py` |
| CapabilityHealth 方法内延迟导入 | 已实现并验证 | ToolManager 支持构造注入，同时保留兼容默认创建路径 | `agent/tools/manager.py` |
| 外部直接修改 yolo/super-yolo | 已实现并验证 | CLI 改用 ToolManager 配置入口 | `agent/tools/manager.py`、`agent/cli.py` |
| Chroma/Playwright 依赖下限不一致 | 已实现并验证 | 两份清单已对齐，`pip check` 与依赖一致性回归通过；wheel 干净安装保留为后续增强门 | 两个依赖清单 |
| 缺 Key 提示推荐明文 `model.yaml` | 已实现并验证 | 错误提示改指向私有 `secrets.env` | `agent/deepseek.py` |
| Launcher 推荐安装所有重型依赖 | 已实现并验证 | 改为核心 editable 安装提示 | `launcher/agent` |
| `runtime.py`、`convergence.py`、`tools/manager.py` 巨型文件 | 延期 | 属于真实技术债，但大拆分风险高，应在 v0.12.0 发布后逐模块、行为等价迁移 | 对应模块 |
| 宽泛 `except Exception` | 部分保留 | Event/可选能力边界需要 fail-safe 宽捕获，但必须记录脱敏错误；逐处缩窄需独立回归 | 多模块 |
| System Prompt 热重载/A-B | 不采纳当前版本 | 热重载和自动试验会降低可复现性；Prompt 变更必须受版本与测试约束 | `agent/prompt.py` |
| 插件化命令、Builder 工具、统一 AppState | 延期 | 不是本次实例卡死的根因，且会扩大公共接口变化面 | CLI/Capability/State |
| 删除废弃别名和旧策略 | 延期 | 需要先确认外部调用兼容性，在明确弃用周期后处理 | `agent/task_strategy.py`、`agent/model_router.py` |

## 七、长期路线材料的版本映射

| 路线目标 | 本轮处置 |
| --- | --- |
| v0.11.1 状态一致性、Key/Vector、CLI 退出码 | 已合并进 v0.12.0 并通过离线验证 |
| v0.12.0 Task Graph、预算、可靠恢复 | 安全子集已实现；并行节点、动态 replan、生命周期预算延期 |
| v0.12.x Document Pipeline | Artifact 结构验证已先行；章节生成与事务式合并延期 |
| v0.13.0 Memory 2.0 | 枚举、容量、保护和显式反馈已先行；LLM 抽取、export/import 延期 |
| v0.14.0 Evidence Engine | Artifact verifier 与 managed validation 已奠基；通用 Evidence DSL 延期 |
| v0.15.0 Durable Intent Journal | 未实现；现有 checkpoint/Resume 不能声明 exactly-once |
| Agent V3 1.0 自主工程交付 | 长期目标，不能在 v0.12.0 文档中宣称完成 |

## 八、参考项目处置

固定参考地址为 `https://gitee.com/free/claude-code/tree/claude/`，本轮记录的参考 commit 为 `b17913e26fd4278ad5cd4b32ed3bde86bf1444e9`。只吸收以下行为原则：工具调用/结果严格配对、输出有界、错误分类、可恢复 transcript/状态、显式并发安全和终端可读反馈。

不直接复制其实现，原因是参考仓库自述为泄露的 Anthropic 专有源码快照且无可复制许可证；同时它的 TypeScript/Claude 多能力架构与本项目 DeepSeek-only Python Runtime 并不等价。
